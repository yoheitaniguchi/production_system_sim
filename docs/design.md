# 生産管理ミニマムシミュレーター 設計書

**版数**: v1（v5仕様書 完全準拠版）
**目的**: 受注情報を入力すると、それが各ドメインへ伝播し出荷に至るまでの流れを、動くシミュレーターとして可視化する
**位置づけ**: 本書は `docs/v5-spec.md`（ユーザー提供「生産管理システム ミニチュア設計書 v5」、以下「v5仕様書」）を
実装仕様の一次資料とする**差分ドキュメント**である。v5仕様書が規定する業務ルール・データモデル・状態遷移・
中核ロジックの疑似コード・受入テストケースはそのまま採用し、本書には次の3種類の情報のみを記載する。

1. v5仕様書に対する**意図的な差分**（§2）
2. v5仕様書が**未規定・曖昧なまま残した点への追加決定**（§3）
3. 本プロジェクト固有の**実装方針**（データモデル対応・画面構成・アーキテクチャ・ディレクトリ構成、§4〜§8）

業務ルールの背景・根拠（なぜその機能が必要か、批判的レビューの経緯等）を確認したいときは `docs/v5-spec.md` を参照すること。

## 0. 読み方

| 知りたいこと | 参照先 |
|---|---|
| 業務ルール（7ドメインの目的・最小機能・組織/現場の2目線） | v5仕様書 第I部（§1〜§5） |
| データモデル・状態遷移・中核ロジックの疑似コード・画面ユースケース・受入テスト・KPI | v5仕様書 第II部（§6〜§10） |
| 拡張ロードマップ（原価・トレーサビリティ等） | v5仕様書 第III部（§11〜§12） |
| 「なぜこの実装なのか」「v5仕様書に無い決定はどうなっているか」 | 本書（design.md） |

## 姉妹リポジトリ `mini-simulator` との関係

`mini-simulator`は小型コンベア装置（多階層BOM、即時発注トリガー、受注専属ペギングによる在庫モデル簡略化、
工程ステップを持たない3状態モデル）を題材にした簡易版を実装している。本リポジトリはそのアーキテクチャ
（React + TypeScript + Vite、バックエンドなし、`useReducer`による状態管理、ドメインロジックを純粋関数として
UIから独立させる設計、D0からの経過日数で日付を扱う等）を踏襲するが、`mini-simulator`とは**データモデル・
操作粒度の前提が異なる独立したプロジェクト**であり、`mini-simulator`のドメインロジック・型定義をそのまま
移植することはしない。詳細な差分は§2で述べる。

---

## §1 スコープ確定

v5仕様書 §1（P1〜P13）のスコープ宣言をそのまま採用する。追加で以下を確定する。

| # | 論点 | 決定内容 |
|---|---|---|
| S1 | 実装フェーズ範囲 | v5仕様書の **Phase 1**（第I部・第II部：7ドメイン・13テーブル）を実装対象の中核とする。~~Phase 2-A（原価）・Phase 2-B（トレーサビリティ）以降は`docs/implementation-plan.md`にロードマップとして記載するに留め、実装しない~~（当初方針）。→ 実装が進み全画面が出揃った段階で、`docs/implementation-plan.md` §5「Phase 7」の一部としてPhase 2-A・Phase 2-Bも実装した（EXT-16・EXT-18参照）。Phase 2-Bは v5仕様書 §11.3が求めるSTOCKの主キー変更（破壊的変更）はEXT-18の判断により見送り、LOT/LOT_GENEALOGYを既存STOCKと並行させる簡略化版として実装している点に注意 |
| S2 | 題材 | v5仕様書 §1.1 の木製イス（FG-100／SA-200／RM-300／PT-400／PT-500、工順3行）をそのまま採用する |
| S3 | 対象受注数 | v5仕様書 §9 の単一受注シナリオ（SO-001-1）に加え、**複数受注が資源を取り合う演習**を追加でv5データモデル上に構成する（`mini-simulator`が持つ同種の演習と同じ狙い。詳細は§6） |

---

## §2 v5仕様書に対する意図的な差分

| # | 差分 | 内容 | 理由 |
|---|---|---|---|
| DEV-1 | PARTNERテーブルを統合しない | v5仕様書 §4は`PARTNER`（`partner_type`で顧客/仕入先を判別）に統合しているが、本実装では`Customer`/`Supplier`を別型として持つ（`mini-simulator`と同じ選択） | 型安全性・画面実装のしやすさを優先。実質的な差はテーブル数13→14のみで、業務ロジックへの影響はない |
| DEV-2 | 自動再生機能は実装しない | `mini-simulator`にある「次の日へ進む」を一定間隔で自動連打する機能は、Phase1では実装しない | §3で確定する通り、Phase1は工程着手/完了・入荷計上・出荷引当/実績・MRP実行・オーダ確定などが個別のユーザー操作になる。「日を進める」だけでは何も進行しなくなるため、自動連打の価値がほぼ失われる。操作が出揃った段階で改めて検討する |
| DEV-3 | 入荷・出荷の数量入力欄を持たない | v5データモデル（`received_qty`/`shipped_qty`の累積管理）は任意数量の分割対応が可能だが、本実装のUIでは**数量入力欄を設けず、1回の操作で「その時点で処理できる分すべて」を自動的に一括処理**する（入荷＝注文残全量、出荷＝受注残と出荷可能量の少ない方）。実装当初は「常に受注残＝発注残の全量を処理する」という誤った解釈で実装し、TC-15（不良により受注残10に対し出荷可能量が9しかないため9個だけ引き当てる）が成立しないことに気付いて修正した | ミニチュアとしてのUI複雑化（数量入力欄）を避けつつ、v5仕様書の受入テスト（TC-15など、出荷可能量が受注残に満たず自然に一部出荷になるケース）を正しく再現するため。データ型自体は将来任意数量を指定する分割対応を妨げない形で持つ |
| DEV-4 | 分析画面のうち「演習ガイド」（v5仕様書§8.1 D3）は先送り | KPIダッシュボード（D1）・ペギング追跡（D2）はPhase1に含めるが、§9のTCを画面上で自動判定・誘導する「演習ガイド」はPhase1.5以降とする | 実装コストが最も高く、`domain/*.test.ts`による自動テスト検証ループで仕様適合の確認はすでに担保できるため |

---

## §3 v5仕様書が未規定・曖昧なまま残した点への追加決定

| # | 論点 | v5仕様書の記述 | 追加決定 |
|---|---|---|---|
| EXT-1 | MRP展開時の需要処理順序 | `runMRP()`疑似コード（§7.1）の`demands`ループは処理順序を規定していない | **需要（`due`＝`confirm_day`）の昇順**にソートしてから`explode()`を呼ぶ。同着の場合は受注登録順（`so_no`昇順）とする。これにより複数受注が同一資材を取り合う際、納期が早い受注から優先的に供給量を消費する |
| EXT-2 | 受注取消時の関連オーダの扱い | §6.1のガード条件は「`shipped_qty == 0` かつ 確定オーダに紐付く実績が無い」とあるのみで、取消後にペグ先のMFG_ORDER/PURCHASE_ORDERをどう扱うかは無規定 | 取消操作の中で、`pegTo`鎖をたどり、**まだ実績（着手実績・入荷実績）が無いMFG_ORDER/PURCHASE_ORDERを連鎖的にCANCELED**へ遷移させる。放置すると「取消済受注のための製造/購買オーダ」という矛盾データが残るため |
| EXT-3 | 取消ガードの厳密な判定基準 | 「確定オーダに紐付く実績が無い」の"実績"の範囲が未定義 | **1件でも**次のいずれかに該当すれば取消不可とする二値判定にする：①ペグ先のいずれかのMFG_ORDERで`actual_start_day`が設定済みのWORK_INSTRUCTIONが存在する、②ペグ先のいずれかのPURCHASE_ORDERで`received_qty > 0`である。中間状態（一部の子オーダだけ実績あり）を許容せず、単純な有無判定に倒す |
| EXT-4 | 入荷計上のタイミング制約 | UC-11「入荷を計上する」に日付ガードの明記なし | 「入荷計上」操作は **`現在日 ≥ (confirm_day ?? due_day)`** の場合のみ実行可能とする。「今日発注していない材料が今日届かない」という因果律を維持するための追加ガード |
| EXT-5 | 出荷実績登録のタイミング制約 | 同上、出荷側は無規定 | ガードを設けない（早期出荷も許容）。納期遵守率KPIが結果として自然に反映するため、人為的な制約は不要と判断 |
| EXT-6 | MRP供給量計算におけるHOLD状態の扱い | §7.1 供給量初期化の`mfgOrders`条件は`status not in [DONE, CANCELED]`とあるのみで、`HOLD`（部品欠品で保留中）を含むかは記述上は含まれる（除外されていない）が明言はない | 字義通り**HOLDも供給に算入する**（除外リストに無いため）。部品欠品で止まっているだけで「作れる見込みがある仕掛残」として扱うのが一般的なMRPの考え方であり、v5の除外条件とも矛盾しない |
| EXT-7 | 納期回答（UC-05）の算出方法 | v5仕様書 §9.1のシーケンス図は「計画管理からの供給可能日」をMRP試算の結果として回答納期に反映する流れを示すが、§9.3の受入テスト表では**TC-03（納期回答）がTC-04（MRP実行）より先**に置かれており、両者の記述順序が矛盾する | シーケンス図側の「MRP試算に基づく自動ATP計算」は実装せず、**納期回答は人間（受注担当者）が希望納期を確認して確定するだけの単純操作**とする（デフォルト値として希望納期を表示し、そのまま確定するか、任意の値に変更して確定できる）。§9.3のTC順序（登録→回答→MRP実行）と整合させるための解釈 |
| EXT-8 | データ増分ログの表示フォーマット | §8.1は「どのテーブルに何行INSERT/UPDATEされたか」を要求するのみで、具体的な表示形式は無規定 | v5仕様書 §8.2のユースケース一覧の「主なデータ変更」列と同一の表記（例：`PLANNED_ORDER −5 / MFG_ORDER +2 / PURCHASE_ORDER +3 / WORK_INSTRUCTION +3`）をログの1行として、業務メッセージ（例：「計画オーダ5件を確定した」）と併記する |
| EXT-9 | 購買オーダの発注先（仕入先）の決め方 | `ITEM`マスタに仕入先を指す列が無く、`PARTNER`（仕入先）も対象品目を指す列を持たない。計画オーダ確定時にどの仕入先で`PURCHASE_ORDER`を起票するかが決定不能 | `ItemMaster`に`defaultSupplierId`（BUY品目のみ設定）を追加し、1品目＝1仕入先で固定する。複数仕入先からの選択・切替（v5仕様書 §5.2「仕入先の切替」が組織目線の判断として言及するもの）はPhase1のスコープ外とする |
| EXT-10 | HOLD状態からの具体的な復帰操作 | §6.3は「制約解消」でHOLD→WIPに戻るとあるのみで、契機となる操作が無い。§7.3 `completeStep()`疑似コードも、部品不足でHOLDにした後の`mo.status`をどう戻すか記述していない | 専用の「再試行」操作は設けない。HOLDになっても当該WORK_INSTRUCTIONの状態はWIPのまま変化しない（ガードは消費前に評価されるため）ため、**部品が充足した後に同じ「工程完了」操作（`WI_COMPLETE`）を同じ工程に対して再実行するだけで自然に成立する**。`completeStep()`側は、呼び出し時に`mo.status === HOLD`であれば処理開始時点で`WIP`に戻してから通常のガード評価を行う |
| EXT-11 | KPI「欠品発生件数」の算出元 | §10は「引当または出庫に失敗した回数」「引当・バックフラッシュの失敗ログ」とあるのみで、失敗イベントを蓄積する専用テーブルは定義していない | 専用の失敗ログテーブルは追加せず、**現在HOLD状態のMFG_ORDER数**を近似値として採用する（バックフラッシュの部品不足はHOLDという形で状態に残るため）。出荷引当の失敗は例外（`ShipmentError`）として即座に呼び出し側へ返るのみで状態に残らないため、この指標には含めない。より厳密な累積カウントが必要になった場合は、Phase 3のreducerで例外発生時にイベントログへ記録し、そこから集計する方式へ拡張する |
| EXT-12 | KPI「計画達成率」「直行率」の集計対象 | §10の定義はMFG_ORDER／WORK_INSTRUCTION全件を対象にしているように読めるが、TC-17の期待値（計画達成率90%・直行率90%）は、中間のサブアセンブリ（座面ASSY、完了時点でgood_qty10/plan_qty10=100%）まで含めて単純合算すると再現できない（含めると計画達成率95%になってしまう） | 両指標とも**末端の受注確定オーダ（`pegTo`が受注のペグキー"SO-xxx-n"形式、＝BOMレベル0）に限定**して集計する。計画達成率はそれらオーダのgood_qty/plan_qtyの合計比。直行率は各オーダについて「最終工程の良品数 ÷ 第1工程の投入数」（複数工程の歩留まりの掛け算に相当）を求め、オーダ横断で合算する。中間工程の出来高は含めない：組織目線のKPIとしても最終製品の顧客向け達成度を測るのが自然という判断 |
| EXT-13 | KPI「在庫回転」の分母（平均在庫数量）の扱い | §10の定義は「期間出庫数量 ÷ **平均在庫数量**」だが、時系列の在庫スナップショットを保持する仕組みは無く、平均値を算出できない | 平均在庫数量の代わりに**現在の総在庫数量**（全品目の`on_hand`合計）を分母として近似する。分母が0（在庫が無い、またはTC-17終了時点のようにすべて出荷・消費済みで手元在庫が無い）場合は`null`を返し、集計不能であることを明示する。より正確な期間平均が必要になった場合は、日次の在庫スナップショットを別途蓄積する仕組みが必要になる（Phase 3.5以降の課題） |
| EXT-14 | KPIダッシュボードの「両方」目線指標の配置 | §10の「主な目線」列は12指標中3指標（計画達成率・製造リードタイム実績・棚卸差異率）を「両方」に分類するが、design.md §5は画面構成を「組織目線/現場目線の別ブロック」の2ブロックとしており、「両方」指標をどちらに置くかは無規定 | どちらか一方に寄せると情報が欠落するため、**「両方」指標は組織目線・現場目線の両方のブロックに重複表示**する（`KpiDashboard.tsx`）。3ブロック目を新設せず、design.md §5の「2ブロック構成」という画面設計をそのまま維持しつつv5仕様書の目線分類を欠落なく反映するための判断 |
| EXT-15 | 品目マスタの売価（sales_price）の初期値 | v5-spec.md §11.2は「ITEM（拡張）：purchase_price、sales_price」と列の存在のみ規定し、具体的な金額の例示は無い（購入単価はrollupCostの計算例から800／250／20と特定できるが、売価は計算例に登場しない） | 木製イス（FG-100、標準原価3,960円）の売価を**6,000円**（約51%の粗利率）と仮置きする。他のBUY品目・SA-200は受注対象外のため売価は0のまま。マスタ画面（`EditableNumberField`、min=0）でいつでも変更可能にすることで、仮置き値であることの実害を無くす |
| EXT-16 | Phase 2-A実装後のKPIダッシュボードへの反映方法 | §11.2「組織目線ダッシュボードへの影響」は、既存12指標のうち3指標（在庫→在庫金額、不良→不良損失額、受注残→受注残高）を数量から金額へ**置き換える**ことを示唆する | 既存の`KpiDashboard.tsx`（12指標、TC-17等で期待値が固定されたテスト済みの画面）は数量ベースのまま変更せず、**新設の`CostPanel.tsx`（原価タブ）に金額指標（在庫金額・受注残高・不良損失額）と品目別標準原価・製造オーダ別原価差異を独立して表示**する。既存KPI画面の破壊的変更を避けつつ、§11.2が求める「金額で語れる」情報は満たすための判断 |
| EXT-17 | 演習ガイド（D3、§8.1）のステップ完了判定方法 | §8.1は「10ステップの進行」とあるのみで、各ステップの完了判定基準・実装方式は無規定 | §9.3のTC-01〜TC-18をそのままステップ一覧とし、各ステップの完了は現在の`SimulationState`から自動判定する（`domain/exerciseGuide.ts`）。TC-04・TC-06・TC-14（3回のMRP実行）は`PLANNED_ORDER`が揮発データで区別できないため、`processFlow.ts`と同じ手法（`eventLog.message`の固定テンプレートを手がかりにする）で「MRPを実行した」ログの出現回数を数える。想定順序（TC-04→TC-06→TC-14）から外れて操作すると回数がずれる場合があるが、本ガイドは進行の目安を示すだけで他タブの操作を妨げないため、簡略化として許容する。TC-13（未充足需要の確認）・TC-17（KPI確認）・TC-18（ペギング追跡）は読み取り専用の確認ステップで状態変化を伴わないため、直前の関連ステップ（TC-12／TC-16）と同時に完了扱いとする |
| EXT-18 | Phase 2-B（トレーサビリティ）実装時のSTOCKの扱い | v5-spec.md §11.3は「STOCK（変更）：item_code + lot_noを主キーに変更」と、既存STOCKの粒度そのものを変える破壊的変更を求める | 既存の`Stock{itemId, onHand, allocated}`（品目単位のfungibleな残高、design.md §4）は主キーを変更せず**そのまま維持**し、実際の入出庫（RCV/PRD/ADJ+でロット生成、ISS/SHP/ADJ-でFIFO消費）と連動する`LOT`・`LOT_GENEALOGY`テーブルを**並行して**追加する（`domain/lot.ts`）。既存の12ドメインモジュール・79件のテストが`state.stocks`の品目単位集計に依存しており、真に主キーを変更すると波及範囲が広すぎるため、「実際に何を使ったか」という後方/前方追跡機能自体は`LOT_GENEALOGY`で正しく実現しつつ、既存コードへの影響を`domain/lot.ts`という追加レイヤに閉じ込める設計とした。副作用として、既存テストが`receivePurchaseOrder()`等のドメイン関数を経由せず`state.stocks`へ在庫を直接注入している箇所ではロットが存在しないため、`consumeFifo()`はロット台帳の残数量で不足する分をエラーにせず`lotNo`未設定（追跡対象外）として扱う。実運用（画面操作）では入出庫は必ずドメイン関数を経由するため、ロットは常に整合した状態で維持される。なお`Lot.qty`は、v5-spec.md §11.3の表が挙げる静的な属性（入庫時の元数量）ではなく、**FIFO消費のたびに減っていく残数量**として運用する（`types.ts`にコメントで明記）。入庫時の元数量が別途必要になった場合は、`LOT_GENEALOGY.consumedQty`の合計と現在の残数量から復元するか、`Lot`に別カラムを追加する形で対応する |
| EXT-18-追記 | 在庫モデルの厳密化検討（STOCKの主キー変更を含む、より厳密な実装への発展の要否） | 「次にやるべきこと」候補②として、EXT-18で見送ったSTOCKの主キー変更（`item_code`単体 → `item_code + lot_no`の複合キー）へ実際に踏み込むべきかを検討した | **踏み込まない（現状維持）と結論した**。理由は3点。①**MRP・引当は本来ロット非依存であるべき**：v5-spec.md §7.1「有効在庫の式とMRPの正味所要計算の式は別物」・design.md §4の在庫モデルコラムが明言するとおり、標準的なMRPは品目単位のfungibleな有効在庫で正味所要を計算し、ロット・シリアル単位の引当は倉庫管理（WMS）側の関心事である。STOCKの主キーをロット複合キーに変えると、`shippableQty()`・`computeSupply()`・`checkSchedule()`等の全ドメインロジックが「品目ごとに1件」という前提を失い、実際には無関係なロット分割の都合でMRP本体まで複雑化する。v5-spec.md自身も§11.3末尾で「MRPの供給量計算はロットを意識しないため、在庫管理側だけが複雑化する」と明記しており、これは推奨ではなく警告として書かれている。②**実運用コードパスでは既に不変条件が成立している**：`state.stocks`のonHandを変更する4箇所（`procurement.ts`の入荷、`production.ts`の部品消費・完成入庫、`shipment.ts`の出荷実績、`inventory.ts`の棚卸調整）は、いずれも同じ関数内で`createLot()`／`consumeFifo()`と対になっており、**「品目ごとのLot.qty合計 === Stock.onHand」という不変条件がreducer経由の操作では常に成立する**ことをコードレビューで確認した（唯一の例外はテストコードが`state.stocks`へ直接注入する箇所で、これはEXT-18が最初から想定・許容している範囲）。この不変条件を`lot.test.ts`に新設した検証テスト（TC-01〜18相当の一連の操作を通しで流し、各チェックポイントで品目ごとに突き合わせる）で明文化・固定化した。③**費用対効果**：主キー変更は20モジュール・175件超のテスト・全画面（特に在庫タブの3列表示）に及ぶ破壊的変更だが、それによって新たに得られる価値（特定ロットを指定した引当・出荷）は、v5-spec.md §9のTC-01〜18・演習の目的（後方/前方追跡＝トレーサビリティの体験）には含まれておらず、教材としての追加学習効果が薄い。**代わりに、破壊的変更なしで実現できる厳密化を2点実施した**：(a) `Lot`に`originalQty`（入庫・完成入庫時点の元数量、不変）を追加した。EXT-18が「別途必要になった場合の対応」として予告していた拡張であり、`qty`（残数量）と対にして`LotTracePanel.tsx`で表示する。(b) 部品消費が複数ロットにまたがるケース（`production.ts`の`completeStep()`がFIFOで2件以上のロットから消費し、単一の生成ロットの系譜に複数の親ロットを記録する経路）について、単発ロットのケースしか検証していなかったテストの穴を`lot.test.ts`に新設したテストで塞いだ。以上により、「主キーを変えない」という既存の判断の妥当性を再検証のうえ明文化し、既存設計の範囲内で可能な厳密化を実施した、というのが今回の検討の結論である |
| EXT-19 | BOM循環参照チェックの実装箇所（自由登録の解禁に伴う） | v5仕様書 §3.7 の最小機能5が「登録時に自身を祖先に持たないことを検証」と要求するが、登録機能そのものが無かったため未実装だった。また循環したBOMを掴んだときの`explode()`（§7.1）・`rollupCost()`（§11.2）の挙動は無規定 | **三重防御**とする。①登録時：`masterIntegrity.wouldCreateBomCycle()`（追加する子から下へDFSし親に到達したら拒否）でv5の要求どおり登録を拒否する。②JSONインポート時：`findBomCycles()`で全件検査し、1件でもあれば取り込まない。③実行時：`explode()`と`rollupCost()`に展開経路の`Set`を持たせ、循環を検出したら例外を投げる（保険。ここを抜けると再帰が止まらずブラウザごと固まり、永続化が無いため演習内容が全消失する）。深さ上限`MAX_BOM_DEPTH = 20`も併せて置く。`traceFromOrder()`・`resolveRootPegKey()`にも訪問済み集合を入れ、pegTo鎖の停止性を保証する |
| EXT-20 | マスタ編集の「禁止」と「警告」の線引き | v5仕様書はマスタの登録・変更を最小機能として挙げるのみで、トランザクションが存在する状態での変更可否を規定していない | **復旧不能・原因不明の停止を生むものだけを禁止し、単に計算結果が変わるだけのものは警告に留める**。禁止：①BOM循環（EXT-19）、②工順が1行も無い内製品目の計画オーダ確定（作業指示ゼロのMFG_ORDERはFIRMのまま永久に完了できないゾンビになる）、③**未完了の製造オーダがある品目の工順の構造変更**（行の追加・削除。`production.ts`の`firstStepNo`/`lastStepNo`/`nextStepNo`は実行時に`state.routingSteps`を読むため、増減させると仕掛中オーダの「最終工程」が変わり完成入庫が起きない／別工程で起きる）、④参照中マスタの削除（EXT-21）、⑤前提を満たさない区分変更（EXT-24）。警告のみ：同じ品目の工順の標準時間・作業区の変更（原価にしか影響しない）と、EXT-23のBOM編集 |
| EXT-21 | マスタ行の削除可否の判定基準 | 無規定（削除機能自体が無かった） | `masterIntegrity.findItemReferences()`等が返す「参照理由の一覧」が空のときだけ削除できる。品目は BOM（親・子）／工順／受注明細／計画オーダ／製造オーダ／購買オーダ／在庫トランザクション／ロット／残高が0でないSTOCK を走査する。**残高0・引当0のSTOCK行は自動生成された空行として無視し、品目と一緒に削除する**。作業区は工順・作業指示、仕入先は品目の既定仕入先・購買オーダ、得意先は受注を走査する。UIでは削除ボタンを`disabled`にし、ブロック理由を`title`で表示する（「なぜ消せないのか」がマスタ画面だけで分かることが、参照整合性を学ぶうえでの主眼） |
| EXT-22 | マスタ健全性の常時チェック | v5仕様書 §7.5 は日程整合・未充足需要の警告のみを定義し、マスタ自体の不整合を見る仕組みは無い | `masterIntegrity.validateMaster()`を追加し、`AlertBar`に日程・需要と並べて常時表示する（導出値なので専用actionは持たない、§7の方針どおり）。検出対象：BOM循環／存在しない品目・作業区への参照／工順が0行の内製品目／既定仕入先が未設定・不存在の購買品目／BOM子行を持つ購買品目（`explode()`が展開しないため死にデータになる）。購入単価の無い購買品目は「警告」（原価0として計算を続行）、それ以外は「エラー」。さらに`firmAllPlannedOrders()`は確定前に全計画オーダを検査し、1件でも該当すれば**何も確定しない**（部分的に壊れた状態を残さない） |
| EXT-23 | 仕掛中オーダがある品目のBOM編集 | 無規定 | **禁止せず、業務メッセージで警告する**（`［注意］未完了の製造オーダ（MO-xxx）の部品消費内容が変わります`）。`production.ts`のバックフラッシュは`state.bom`を実行時参照するため消費内容が実際に変わるが、オーダが停止するわけではない。v5仕様書 §3.7 が主題として掲げる「マスタの品質が計算結果の品質の上限を決める」の実演として、教材上はむしろ見せる価値がある |
| EXT-24 | コード（主キー）の変更可否と区分（MAKE/BUY）の変更条件 | 無規定 | 品目コード・作業区コード・取引先番号は**作成後に変更できない**。改名は「削除→再登録」で行う（参照中は削除禁止なので安全に倒れる）。12テーブルのFKを一斉に書き換えるカスケードは、教材の実装量に対して得るものが無いと判断した。区分（MAKE/BUY）の変更は、①未完了のMFG_ORDER/PURCHASE_ORDERが無く、②MAKE→BUYの場合はBOM子行・工順行が0行であること（残っていると死にデータ／完了不能オーダになる）を条件に許可する。BUY品目には既定仕入先を必須とし、`firmAllPlannedOrders()`が`supplierId: ""`を黙って書いていた穴を塞ぐ。**追記（マージ後レビュー）**：`domain/masterData.ts`のこの制約（BUY化には`defaultSupplierId`が同一patch内に必要）に対し、`ItemMasterTable.tsx`の区分セルが単一フィールドpatchしか送らないバグがあり、画面からMAKE→BUYへ到達不能だった。区分セルの`onChange`が「BUYへ切替時は`state.suppliers[0]`を仮の既定仕入先として同一patchに含める」よう修正し、以後は右隣のセルで選び直せるようにした |
| EXT-25 | 画面のローカル状態が削除済みマスタを指す場合 | 無規定（マスタが固定だったため発生しなかった） | `SalesOrderPanel`・`InventoryPanel`は`useState`の初期値として品目・得意先IDを保持しているため、選択中のマスタが削除されると存在しないIDが残る。**レンダリング時に`state`へ存在するか検証し、無ければ先頭要素へ読み替える**（`useEffect`での同期はしない）。加えてドメイン側（`createSalesOrder()`）にも品目・得意先の存在チェックを置き、UIを信頼しない構えにする |
| EXT-26 | 題材の持ち出しと既定シナリオの維持 | v5仕様書は題材を木製イス1本に固定しており、複数題材の切替は想定していない | 木製イス（`data/masterData.ts`）を**既定プリセット**（`CHAIR_PRESET`）として維持し、`createInitialState()`の戻り値は従来どおりとする。これにより §9 のTC-01〜18・演習ガイド・既存の自動テストは無変更で通る。別題材は`MasterSnapshot`型のJSONとして入出力する（`domain/masterIO.ts`。バックエンドが無いのでBlobダウンロードとFileReaderで実現し、localStorageへの永続化はしない）。取り込みは**all-or-nothing**（スキーマ検証＋`validateMaster`のエラーを全件通す）。取り込み・プリセット復元はいずれも既存トランザクションの品目コードと整合しないため、`RESET`と同じく**全トランザクションを初期化する**。**追記（マージ後レビュー）**：当初のスキーマ検証（`requireNumber`）は有限数チェックのみで、CRUD側（`masterData.ts`）が課す範囲制約（`qtyPer`は正、`leadTimeDays`・`stdTimeMin`・`ratePerHour`・`purchasePrice`・`salesPrice`は0以上、`stepNo`は正の整数）を見ておらず、「all-or-nothingのはずが、CRUDでは拒否される値がインポートだけ素通りする」穴があった（特に負の`qtyPer`は`production.ts`のバックフラッシュで在庫が減るはずが増える実害に繋がる）。`requireNonNegative`/`requirePositive`/`requirePositiveInt`等をCRUD側と同じ強さで課すよう修正した |
| EXT-27 | 演習ガイドとプリセット以外のマスタの関係 | 無規定 | `exerciseGuide.ts`はv5-spec.md §9の木製イスシナリオ専用の判定（`ITEM_IDS`直指定）を持つため、別題材のマスタでは判定が成立しない。判定ロジックは変更せず、`isPresetMaster()`（品目コード集合の一致判定）が偽のとき`ExerciseGuidePanel`の先頭に「現在のマスタは演習用プリセットと異なります」という案内を出し、「既定プリセットに戻す」への導線を示すに留める |
| EXT-28 | 仕掛品（BOM下位のMAKE品目）自身の工程で不良が出た場合のMRP再展開 | §9.4のTC-12〜14は不良が受注に直結する末端品目（FG-100）自身の最終工程で発生するケースのみを扱う。仕掛品（SA-200等）自身の工程で不良が出た場合の`runMRP()`の挙動は無規定 | 当初の実装は、EXT-6（HOLDも供給に満額算入する決定）とのすり合わせが不十分だった。SA-200自身の完了実績で欠けた分は、SA-200を消費するFG-100のMFG_ORDERを`completeStep()`が部品不足でHOLDにするだけで、`computeSupply()`はそのHOLDオーダ自体を`planQty`満額の供給として数え続ける。`explode()`は受注需要をその品目自身の供給で相殺できた時点（`netQty<=0`）で即returnしBOM下位を見に行かないため、FG-100の受注需要はHOLD中オーダの満額供給で「充足済み」と誤判定され、SA-200・RM-300の不足がMRP再実行でもunmetDemand()でも検知されないというギャップがあった。**対策として`explodeOpenOrderComponents()`（`mrp.ts`）を追加**し、DONE/CANCELED以外の全MAKEのMFG_ORDER（HOLDを含む）について、残数量（`planQty-goodQty`）分のBOM部材所要を`runMRP()`のたびに`explode()`へ流し込み、供給不足があれば補充のPLANNED_ORDERを生成するようにした（SO駆動の需要展開より先に処理し、同じ供給プールを共有する）。子品目の必要日はそのオーダ自身の`startDay`、pegToはそのオーダ自身の`ploNo`とし、既存のペギング解決（`resolveRootPegKey`）とそのまま整合させた。EXT-6の「HOLDは供給に満額算入する」という決定自体は変更していない（FG-100は依然として満額供給と見なされ、新規のFG-100計画オーダは生成されない）。変わったのは、その満額供給の前提となる下位品目の在庫的な裏付けを、既存オーダ発生時点のスナップショットのままにせず毎回再検証するようにした点である。**追記（レビュー後の是正）**：`logic-reviewer`によるレビューで2点の指摘を受け、対応した。①`bomLevel`（§8.3が要求する「計画オーダのペグ先とBOMレベル」表示の根拠）が、既存オーダ自身の真の深さに関わらず常に0始まりで再採番されてしまい、TC-04（SA-200=1、RM-300=2）が示す既存の意味づけと矛盾していた。`MfgOrder`に`bomLevel`フィールドを追加して`firmAllPlannedOrders()`で確定元の`PlannedOrder.bomLevel`をそのまま引き継ぐようにし、`explodeOpenOrderComponents()`はその既存オーダ自身の`bomLevel + 1`を子品目の階層として渡すよう修正した。②複数の既存オーダが同一の下位品目を取り合う場合の処理順序が無方針だった（`state.mfgOrders`の配列順＝確定順のまま処理していた）ため、EXT-1と同じ規則（必要日昇順、同着はオーダ番号昇順。ここでの必要日はオーダ自身の`dueDay`）でソートしてから処理するよう変更し、完了予定日が早い既存オーダを優先して供給プールを消費させることにした |
| EXT-29 | 進捗ガントチャート（受注ごとの計画・実績比較画面）の表示粒度・対象範囲 | §9.2に単一受注の日程逆算をmermaid ganttで示す例はあるが、複数受注を一覧する画面としての仕様は無規定（§8.1の画面区分にも記載が無い）。§8のディレクトリ構成には`domain/gantt.ts`（受注一覧ガントチャート）が計画として記載されていたが、実装は先送りされていた | ユーザーとの確認の上、以下を決定した。①**行構成**：受注1行（受注〜出荷全体の要約バー）を既定表示とし、行頭の▸をクリックすると`traceFromOrder()`（`pegging.ts`）が返すのと同じ確定オーダ集合（MFG_ORDER/PURCHASE_ORDER）に加えSHIPMENTを子行として時系列に展開する階層表示にする（引当元追跡と同じ集合を、ツリーではなく時間軸で見せる）。②**計画と実績の重ね合わせ**：同じ行内に計画（破線の枠バー）と実績（塗りバー）を並べ、実績が計画終了日を超えていれば「遅延」として配色を変える。この「遅延」判定は`schedule.ts`の`checkSchedule()`（ペグ先間の日程整合警告）とは別概念の、バー単体の視覚表現に留める。③**対象範囲**：全受注を既定表示し、完了済み（CLOSED）・取消済み（CANCELED）をチェックボックスで除外できるようにする。④**横軸**：他画面と同じくD+nの相対日数軸とし、暦日への変換はしない（v5-spec.md §9.2のサンプルは暦日だが本実装では不採用）。⑤`firmAllPlannedOrders(state, day)`は`PurchaseOrder.orderDay`にMRPが逆算した`PlannedOrder.startDay`ではなく、確定操作を実行した`day`をそのまま書き込む実装になっているため、PurchaseOrderタスクの計画開始日と実績開始日は常に同一になる（両者の乖離は終了日側にのみ現れる）。⑥SHIPMENTは期間ではなく一時点のイベントのため、区間バーではなくダイヤ型のマイルストーンマーカーとして描画する。⑦取消済み（CANCELED）の受注はEXT-2・EXT-3により実績が一切無い状態でしか成立しないため、実績バーを持たせず常に「取消」表示のみとする |
| EXT-30 | 能力計画（CRP）の負荷計算粒度 | v5仕様書は§11.1のロードマップでPhase 3として「能力所要量計画（CRP）：作業区の負荷と山積み」を挙げるが、Phase 2-A（§11.2）・Phase 2-B（§11.3）と異なり最小設計（追加テーブル・ロジック・計算例）を書いていない | 本書§9で最小設計を新設する。要点：①有限能力スケジューリング（山崩し・自動リスケジュール）は行わず、**山積み（負荷の可視化と超過検知）のみ**を対象とする（P13・EXT-20の「警告のみ」線引きを踏襲）。②`WORK_INSTRUCTION`はMFG_ORDER単位の`startDay`/`dueDay`しか持たず工程ごとの計画日が無いため、**計画負荷はオーダの着手日（`mo.startDay`）へ工程ごと一括計上**する近似とする。③未着手（WAIT）工程の計画負荷数量は`wi.inputQty`（前工程完了まで0のまま）ではなく`mo.planQty`を使う（歩留まり100%前提はMRP本体と一貫）。④実績負荷は工程ごとの実際の着手日（`wi.actualStartDay`）を使うため、計画/実績で粒度が異なる非対称設計になるが、実行前後で得られる情報の違いに起因する意図的な設計として許容する |
| EXT-31 | 能力超過の扱いとAlertBar連携 | 無規定 | 能力超過は**警告のみ**とし、確定（`firmAllPlannedOrders`）・リリース・着手などの既存操作を一切ブロックしない（EXT-20の「復旧不能な停止を生むものだけ禁止」という線引きに合わせる）。`checkSchedule`/`unmetDemand`/`validateMaster`と同様、`AlertBar`に山積み超過の警告を追加する（専用actionは持たない、導出値）。KPIダッシュボード（`kpi.ts`、TC-17で期待値固定）は変更せず、独立した新規モジュール`domain/capacity.ts`・新規タブ`CapacityPanel.tsx`として追加する（EXT-16と同じ判断：既存の検証済み画面を壊さない） |
| EXT-32 | 作業区マスタへの新規必須フィールド追加とJSON入出力の互換性 | 無規定 | `WorkCenter`に`capacityMinPerDay`（1日あたり稼働可能分数）を追加する。マスタCRUD（`masterData.ts`）・JSON入出力（`masterIO.ts`）は既存の`ratePerHour`と同じ強さのガード（0以上）を課す。**この結果、本フィールド追加前にエクスポート済みのJSONファイル（`capacityMinPerDay`を持たない）は再インポートできなくなる**（値を欠落時に既定補完する特別扱いはしない。既存の`requireNonNegative`の一貫性を優先し、本アプリがセッション限りの教材でありエクスポートは長期保存用途ではないことから許容できる影響と判断した） |
| EXT-33 | 山積み超過（CRP）を解消する手段の実装（製造オーダの分割） | §9.2は「CRPは見せる機能であり解く機能ではない」とし、有限能力スケジューリング（自動リスケジュール）を明確に禁止するが、超過を確認した後に**人が判断して行う手動の解消操作**そのものは無規定 | `production.ts`に`splitMfgOrder(state, moNo, splitQty, newStartDay, newDueDay)`を追加し、`MFG_SPLIT`actionで発火する（`ProductionPanel.tsx`）。FIRM（未リリース）の製造オーダのみを対象に数量を2件へ分割し、分割先を任意の着手日／完了予定日へ再配置できる。これは自動リスケジュールではなく人が数値を指定して発動する操作であるため、§9.2のP13踏襲（「有限能力スケジューリングは行わない」）と矛盾しない。`capacity.ts`の山積み計算・`AlertBar`の警告表示は状態を保持しない導出値（EXT-31）のため、分割によってオーダの`startDay`・`planQty`が変われば**追加の実装なしに次の再計算で自動的に警告が消える**。分割後の新オーダは元オーダと同じ`ploNo`・`pegTo`を引き継ぐため、`pegging.ts`のペグ先追跡・`gantt.ts`のガント表示（いずれも`traceFromOrder()`の結果をそのまま並べるだけの設計）は変更せずに複数オーダ表示へ自然に対応する。一方`schedule.ts`の`checkSchedule()`は`ploNo`をキーに`state.mfgOrders.find()`で「親オーダ」を1件だけ引くため、分割後は同じ`ploNo`を持つ2件のうち配列内で先に現れる方（＝分割元の残数量オーダ）が採用され、新設した分割先オーダの日程は日程整合チェックの対象に含まれない近似となる。L-C1（計画負荷はオーダ単位の近似）と同種の限界として許容する。リリース後（RELEASED以降）の分割は対象外とする：作業指示の投入数・実績が積み上がり始めており、分割時の再配分ルール（どちらに実績を引き継ぐか）が自明でなくなるため |

---

## §4 データモデル対応（v5の13テーブル → TypeScript型）

v5仕様書 §4 のER図をそのままTypeScriptの型定義に対応させる。命名は`camelCase`に変換し、日数フィールドは
すべて`number`（D0からの経過日数、v5仕様書 §4後注のとおり）とする。

| v5テーブル | 対応する型（`src/types.ts`） | 備考 |
|---|---|---|
| `ITEM` | `ItemMaster` | `uom`は全品目「個」固定（P10）のためフィールドとして持たない |
| `BOM_LINE` | `BomLine` | |
| `ROUTING_STEP` | `RoutingStep` | `itemId, stepNo, workCenter, stdTimeMin` |
| `PARTNER` | `Customer` / `Supplier` | DEV-1により分離を維持 |
| `SALES_ORDER` + `SO_LINE` | `SalesOrder` + `SoLine` | データモデルは2テーブルに分離するが、**1受注＝1明細固定**（`SalesOrder`生成時に`SoLine`を必ず1件同時生成する）。`peg_to`のキー形式（`SO-001-1`）はそのまま採用し、ペギング追跡画面での表示に使う |
| `PLANNED_ORDER` | `PlannedOrder` | 状態を持たない揮発データ。MRP実行のたびに配列を丸ごと再生成する |
| `MFG_ORDER` | `MfgOrder` | 状態は§6.3のステートマシンをそのまま採用（FIRM/RELEASED/WIP/HOLD/DONE/CANCELED） |
| `WORK_INSTRUCTION` | `WorkInstruction` | 状態はWAIT/WIP/DONE |
| `PURCHASE_ORDER` | `PurchaseOrder` | 状態はORDERED/ACKED/PARTIAL/CLOSED/CANCELED |
| `STOCK` | `Stock` | 品目単位（`itemId`）で`onHand`/`allocated`を持つ、fungibleな残高管理（下記コラム参照） |
| `STOCK_TXN` | `StockTxn` | RCV/ISS/PRD/SHP/ADJの5種。`onOrder`は列として持たず、常にPURCHASE_ORDERから導出する（v5仕様書 §3.5・R4-5のとおり） |
| `SHIPMENT` | `Shipment` | 状態はALLOCATED/SHIPPED/CANCELED |

### 在庫モデルについて

在庫（STOCK）は品目単位のfungibleな残高管理（`on_hand`／`allocated`）に統一し、ペギング（`peg_to`鎖）は
**追跡専用の別レイヤ**として物理在庫の取り合いには関与させない（v5仕様書 §7.4の設計判断そのもの）。
`mini-simulator`が採用している「仕掛品・完成品を受注ごとにペグして二値管理する」簡略化（受注専属の
MTOだから成立する簡略化）は、本プロジェクトでは採用しない。これはv5仕様書が明確に主張する標準的な
MRPの考え方（§7.1「有効在庫の式とMRPの正味所要計算の式は別物」）であり、教材としての難度は上がるが、
実務のMRPシステムにより近い体験を優先する。UI側では工程画面・ペギング追跡画面で「受注ごとの状態」を
見せることで、受注単位の見やすさを確保する。

---

## §5 画面構成の実装対応

v5仕様書 §8.1（共通シェル／ドメイン画面／分析画面）をそのまま画面構成の骨格として採用する。

| v5仕様書の区分 | 画面 | 対応コンポーネント | 備考 |
|---|---|---|---|
| 共通シェル | シミュレーション時計 | `ClockControls.tsx` | 「日を進める」はUC-22のみを行う（`day += 1`）。自動再生はDEV-2により無し |
| 共通シェル | 警告バー | `AlertBar.tsx` | `checkSchedule()`・`unmetDemand()`・`validateMaster()`（EXT-22）の結果を都度計算して表示。専用ボタンは無い（読み取り専用のため） |
| 共通シェル | データ増分ログ | `EventLogPanel.tsx` | EXT-8のフォーマットで1操作＝1エントリ |
| ドメイン画面 | 1 受注 | `SalesOrderPanel.tsx` | 受注登録・納期回答（EXT-7）・取消（EXT-2, EXT-3） |
| ドメイン画面 | 2 計画 | `PlanningPanel.tsx` | MRP実行（UC-06）・計画オーダ一括確定（UC-07）・計画オーダ一覧（ペグ先・BOMレベル表示） |
| ドメイン画面 | 3 発注 | `ProcurementPanel.tsx` | 仕入先納期回答登録（UC-10）・入荷計上（UC-11、EXT-4のガード付き）・注文残一覧 |
| ドメイン画面 | 4 工程 | `ProductionPanel.tsx` | 製造オーダのリリース（UC-12）・分割（EXT-33、FIRMのみ）・工程着手/完了（UC-13/14、良品数・不良数を並べて入力） |
| ドメイン画面 | 5 在庫 | `InventoryPanel.tsx` | 現在庫・引当済・出荷可能量を3列表示。棚卸調整（UC-17） |
| ドメイン画面 | 6 出荷 | `ShipmentPanel.tsx` | 出荷指示＝引当（UC-18）と出荷実績登録（UC-19）を別操作として提供 |
| ドメイン画面 | 7 マスタ | `MasterDataPage.tsx` ＋ `components/master/*` | 品目・BOM・工順（BOP）・作業区・取引先の**フルCRUD**（EXT-20〜EXT-24）。BOMはUC-02が求めるツリー表示を併設。JSON入出力とプリセット復元（EXT-26）。テーブルごとに`ItemMasterTable`/`BomTable`/`RoutingTable`/`WorkCenterTable`/`PartnerTable`へ分割 |
| 分析画面 | D1 KPIダッシュボード | `KpiDashboard.tsx` | v5仕様書 §10 の12指標を組織目線/現場目線の別ブロックで表示 |
| 分析画面 | D2 ペギング追跡 | `PeggingTracePanel.tsx` | `traceFromOrder()`（§7.4）を実装し、受注→計画→確定オーダ→トランザクションを辿るツリー表示 |
| 分析画面 | D3 演習ガイド | （Phase1.5に先送り、DEV-4） | — |
| （付加価値） | プロセス連携図 | `ProcessFlowDiagram.tsx`（`ProcessFlowPopup.tsx`でポップアップ表示） | v5の7ドメイン・IPO（§2・§3）に合わせたBPMN風の連携図。ペギング追跡（受注単位の系譜）とは役割が異なるため両立させる。他タブを操作しながら参照できるよう、タブではなくフローティングポップアップとして表示する（App.tsxの`app__tabs`内のトグルボタンで開閉） |
| （付加価値） | 進捗ガント | `GanttChartPanel.tsx`（`domain/gantt.ts`） | 受注ごとに受注〜出荷の計画（枠線バー）と実績（塗りバー）を時間軸で並べて比較するガントチャート（EXT-29）。ペギング追跡（D2、`traceFromOrder()`が返す確定オーダ集合をツリーで見せる）と同じ集合を、こちらは時系列で見せる。行を展開すると購買・製造・出荷オーダを子行として表示する |
| （付加価値） | 能力（山積み） | `CapacityPanel.tsx`（`domain/capacity.ts`、未実装・§9で計画のみ確定） | v5仕様書ロードマップPhase 3（CRP、§11.1）に対応。作業区×日で計画負荷・実績負荷・能力を一覧し、超過をハイライトする（EXT-30〜32）。有限能力スケジューリングはしない（§9.2） |

---

## §6 演習シナリオ

- v5仕様書 §9.3 の TC-01〜TC-18、§9.5 の TC-E1〜TC-E3 を、番号・期待値をそのまま `domain/*.test.ts` に
  自動テストとして書き起こす。処理順序・タイミングは§2・§3の追加決定（特にEXT-1・EXT-4・EXT-7）を反映する
- 追加で、複数受注が同一資材を取り合う演習（TC-M1〜）を新設する。律速資材は
  **RM-300（木板・LT5日）** を軸に構成する。v5仕様書 §9.2 の単一受注ワークサンプルにおいて、RM-300は
  すでに「発注D+7・必要D+12」という着手日基準のクリティカルパス上にあるため、これを2受注に拡張する形が
  最も自然と判断した
- 具体的な日数表はEXT-1の需要ソート順を踏まえて実装時に`*.test.ts`へ書き起こし、手検証しながら確定する

---

## §7 実装アーキテクチャ

バックエンドサーバーを持たず、React/TypeScriptのみで完結させる。永続化しない（DBなし）、単一セッション、
シンプルなリクエスト・レスポンスという前提のため、サーバーを置く必然性がない（`mini-simulator`と同じ判断）。

| 層 | 役割 | 実装 |
|---|---|---|
| ドメインロジック | 7ドメインの処理（MRP展開・バックフラッシュ・引当・ペギング・KPI算出など） | 純粋なTypeScript関数群。副作用を持たず、状態を受け取って次の状態を返す |
| 状態管理 | 現在の全ドメイン状態 | ReactのuseReducerで一元管理 |
| 画面 | 状態を表示し、ユーザー操作を受け付ける | Reactコンポーネント |

### action一覧（v5仕様書 §8.2 のユースケースをactionへ対応させたもの）

| action | 対応UC | 役割 |
|---|---|---|
| `SO_CREATE` | UC-04 | 受注登録（`SalesOrder`+`SoLine`を1件ずつ同時生成） |
| `SO_CONFIRM_DELIVERY` | UC-05 | 納期回答確定（EXT-7） |
| `SO_CANCEL` | — | 受注取消（EXT-2・EXT-3のカスケード込み） |
| `MRP_RUN` | UC-06 | `PLANNED_ORDER`全削除→再生成（EXT-1の順序で展開） |
| `PLANNED_ORDERS_FIRM` | UC-07 | 全計画オーダを一括確定（`MFG_ORDER`/`PURCHASE_ORDER`/`WORK_INSTRUCTION`生成） |
| `PO_ACK` | UC-10 | 仕入先納期回答登録 |
| `PO_RECEIVE` | UC-11 | 入荷計上（EXT-4のガード、DEV-3により全量一括のみ） |
| `MFG_RELEASE` | UC-12 | 製造オーダのリリース |
| `MFG_SPLIT` | —（EXT-33） | 製造オーダの分割（FIRMのみ対象）。数量を2件に分け、分割先を別の着手日／完了予定日へ配置する。山積み超過の解消などに使う人による手動操作 |
| `WI_START` | UC-13 | 工程着手 |
| `WI_COMPLETE` | UC-14 | 工程完了（良品数・不良数入力、第1工程ならバックフラッシュ） |
| `STOCK_ADJUST` | UC-17 | 棚卸調整 |
| `SHIPMENT_ALLOCATE` | UC-18 | 出荷指示（引当） |
| `SHIPMENT_SHIP` | UC-19 | 出荷実績登録 |
| `ADVANCE_DAY` | UC-22 | `day += 1`のみ |
| `RESET` | UC-23 | 全トランザクション初期化（マスタは保持） |
| `MASTER_ADD_*` / `MASTER_UPDATE_*` / `MASTER_DELETE_*` | UC-01〜03（編集拡張） | 品目・BOM行・工順行・作業区・取引先のCRUD。フィールドごとにactionを増やさず、**エンティティごとに ADD / UPDATE(patch) / DELETE の3本**へ統一する（EXT-20〜EXT-24のガードは`domain/masterData.ts`側で評価する） |
| `MASTER_IMPORT` / `MASTER_RESET_TO_PRESET` | —（EXT-26） | マスタ一式の差し替え。既存トランザクションと整合しないため`RESET`同様に全トランザクションを初期化する |

`checkSchedule()`（UC-08）・`unmetDemand()`（UC-09）・`validateMaster()`（EXT-22）・KPI（UC-20）・
`traceFromOrder()`（UC-21）はいずれも状態から都度計算する導出値であり、専用のactionを持たない。
マスタのJSONエクスポート（`exportMasterSnapshot()`）も状態を変えないためactionを持たない。

---

## §8 ディレクトリ構成

```
production_system_sim/
├── CLAUDE.md
├── README.md
├── docs/
│   ├── v5-spec.md            # v5仕様書（一次資料、原文のまま）
│   ├── design.md             # 本書（v5との差分・追加決定・実装方針）
│   └── implementation-plan.md
├── src/
│   ├── types.ts              # §4のデータモデル対応をそのまま型定義に
│   ├── data/
│   │   └── masterData.ts     # v5仕様書 §1.1（木製イス）＝既定プリセット CHAIR_PRESET（EXT-26）
│   └── domain/
│       ├── masterData.ts     # マスタCRUD（v5 §3.7、EXT-20〜EXT-24）
│       ├── masterIntegrity.ts # BOM循環・参照検査・健全性チェック（v5 §3.7 最小機能5、EXT-19/21/22）
│       ├── masterIO.ts       # マスタ一式のJSON入出力（EXT-26）
│       ├── mrp.ts            # runMRP/explode/firmAllPlannedOrders（v5 §7.1）
│       ├── production.ts     # startStep/completeStep（バックフラッシュ、v5 §7.3、EXT-10）
│       ├── procurement.ts    # PO納期回答・入荷計上（v5 §6.5、EXT-4）
│       ├── shipment.ts       # allocate/shipOut/shippableQty（v5 §7.2、EXT-5）
│       ├── salesOrder.ts     # 受注登録・納期回答・取消（v5 §6.1、EXT-2/3/7）
│       ├── pegging.ts        # traceFromOrder（v5 §7.4）
│       ├── schedule.ts       # checkSchedule/unmetDemand（v5 §7.5）
│       ├── inventory.ts      # 棚卸調整（v5 UC-17。実装時に8モジュール構成では収まらないと判明し追加）
│       ├── kpi.ts            # v5 §10のKPI算出（EXT-11）
│       ├── gantt.ts          # 受注ごとの進捗ガントチャート用データ計算（EXT-29）
│       ├── capacity.ts       # 能力計画（CRP）の山積み計算（未実装・§9で計画のみ確定、EXT-30〜32）
│       ├── processFlow.ts    # プロセス連携図
│       └── reducer.ts        # 上記モジュールをactionへディスパッチ
│       （各ファイルに対応する `*.test.ts` を併設）
└── components/                # §5の対応表のとおり
```

---

## §9 Phase 3：能力計画（CRP）最小設計（v5仕様書ロードマップの追加決定）

v5仕様書 §11.1は能力所要量計画（CRP）を拡張ロードマップのPhase 3として位置づけるが（§12用語対応表にも
「Capacity Requirements Planning | CRP | Phase 3」とある）、Phase 2-A（§11.2）・Phase 2-B（§11.3）と異なり
最小設計（追加テーブル・ロジック・計算例）は書かれていない。本節はその欠落を埋める、design.mdとしての
追加決定である（EXT-30〜32参照）。**本節時点では設計のみを確定し、実装はまだ行っていない**。実行可能な
タスクへの分解は`docs/implementation-plan.md`「Phase 8：能力計画（CRP）」を参照。

### 9.1 目的

v5仕様書 §1 P2は「無限能力。工程の負荷は計算しない」とスコープ外を宣言しているが、§11.1は同時に
「能力所要量計画（CRP）：**なぜ計画通りに作れないのか**の最大要因。ボトルネックと段取り」とその欠落の
重大さも述べている（§15 L1にも「無限能力前提のため、なぜ計画通りに作れないのかの最大要因が体験できない」
と残存リスクとして明記されている）。CRPの目的は、この欠落を埋め、**現時点のMRP計画・製造実績が作業区の
実際の処理能力を超えていないかを可視化し、超過があれば警告する**ことである。

### 9.2 スコープ境界（何をしないか）

無限能力前提（P2）そのものは変更しない。CRPは「見せる」機能であり「解く」機能ではない：

- **有限能力スケジューリング（山崩し・平準化・オーダの自動リスケジュール）は行わない**。P13（自動リスケジュールなし）の方針をそのまま踏襲する
- **稼働日カレンダー（土日祝日・複数直・稼働率）は導入しない**。D0からの経過日数モデル（v5仕様書§4後注）には曜日という概念自体が存在しないため、作業区の稼働能力は「1日あたりの固定値」のみとする
- `runMRP()`のリードタイムオフセット計算（`item.leadTimeDays`）は能力を考慮しない。能力超過が分かっても、MRPが自動的に着手日をずらすことはない
- KPIダッシュボード（`kpi.ts`、v5-spec.md §10の12指標）は変更しない。能力関連の指標を追加する必要が生じた場合も、原価と同じ判断（EXT-16）に従い独立パネルで対応する

### 9.3 データモデル拡張

| 型 | 追加フィールド | 内容 |
|---|---|---|
| `WorkCenter`（`types.ts`） | `capacityMinPerDay: number` | 作業区の1日あたり稼働可能時間（分）。`stdTimeMin`と単位を揃え、時間↔分の変換誤差を避ける |

新規テーブルは追加しない（`WorkCenter`は既にPhase 2-Aで追加済み、EXT-15参照）。山積み結果自体は状態を
持たない導出値（`checkSchedule`/`unmetDemand`と同じ設計）とし、`SimulationState`には保存しない。

### 9.4 山積み計算ロジック（疑似コード）

```
interface CapacityLoadEntry {
  workCenter: string
  day: number
  plannedMin: number   // 計画負荷：未着手の作業指示をオーダの着手日へ一括計上
  actualMin: number    // 実績負荷：実際に着手した作業指示を実着手日へ計上
  capacityMin: number  // その作業区の1日あたり能力
}

function computeCapacityLoad(state) -> CapacityLoadEntry[]:
    buckets = Map<(workCenter, day), {plannedMin, actualMin}>

    for wi in state.workInstructions:
        mo = state.mfgOrders.find(moNo == wi.moNo)
        step = state.routingSteps.find(itemId == mo.itemId, stepNo == wi.stepNo)
        if wi.actualStartDay != null:
            # 実績負荷：着手済みの投入数（既知）をそのまま使う
            buckets[(wi.workCenter, wi.actualStartDay)].actualMin += wi.inputQty * step.stdTimeMin
        else if mo.status not in [DONE, CANCELED]:
            # 計画負荷：未着手工程はwi.inputQtyが0のままのため使えない（9.7 C2-1参照）。
            # mo.planQty（歩留まり100%前提）をオーダの着手日へ一括計上する
            buckets[(wi.workCenter, mo.startDay)].plannedMin += mo.planQty * step.stdTimeMin

    return [ {workCenter, day, plannedMin, actualMin, capacityMin: workCenterOf(workCenter).capacityMinPerDay}
             for (workCenter, day), {plannedMin, actualMin} in buckets ]
        .sortBy(day, workCenter)

function capacityOverloads(state) -> CapacityLoadEntry[]:
    return computeCapacityLoad(state).filter(e => e.plannedMin > e.capacityMin || e.actualMin > e.capacityMin)
```

計画負荷と実績負荷は、1件の作業指示につきどちらか一方にのみ計上される（着手済みか否かで排他的に分岐する
ため、二重計上は起きない）。DONEになった工程も実績負荷（実着手日）として残り続ける（`StockTxn`同様、
実績は事後に消えない）。

### 9.5 具体的な計算例（木製イスデータ、TC-04〜05の確定結果をそのまま使う）

3作業区とも`capacityMinPerDay = 240`（実働4時間相当）とする。8時間（480分）にすると既定シナリオ
（SO-001-1、木製イス10個）単体では一度も山積み超過が発生せず、CRPの学習効果を体験する演習が既定シナリオ
の外に出てしまうため、意図的に低い値を選ぶ（v5-spec.md §9.4の不良1個演習が数値を意図的に設計しているのと
同じ考え方）。

v5-spec.md §9.3 TC-04の確定結果（本書§3 EXT-28引用元のPLOテーブル）をそのままfirmすると：

| 作業区 | 対象オーダ | 日 | 必要時間 | 能力 | 判定 |
|---|---|---|---|---|---|
| WC-CUT | MO（SA-200 x10、工程10） | D+12 | 10個×18分=180分 | 240分 | OK |
| WC-ASM | MO（FG-100 x10、工程10） | D+13 | 10個×30分=300分 | 240分 | **超過 +60分** |
| WC-INS | MO（FG-100 x10、工程20） | D+13 | 10個×12分=120分 | 240分 | OK |

TC-04〜05の操作（受注→MRP実行→計画オーダ確定）をそのまま実行するだけで、追加のシナリオ設計なしに
WC-ASMの山積み超過が再現できる。この数値を実装時の`capacity.test.ts`の期待値として使う。

### 9.6 実装可能性・整合性レビュー（第1周：スコープ・設計方針）

実装前の批判的レビューとして、経験ある生産管理システムエンジニアの視点（v5-spec.md §13と同じ観点）で
2周実施した記録を残す。

| # | 指摘 | 深刻度 | 対応 |
|---|---|---|---|
| C1-1 | 「能力計画」を有限能力スケジューリング（山崩し・自動リスケジュール）まで含めて実装しようとすると、P13・EXT-20の「警告のみ」線引きと衝突し、シミュレータの性格（判断は人がする）自体を変えてしまう | 高 | §9.2でスコープを「山積み（可視化・超過検知）」のみに限定し、確定・リリース・着手などの既存操作は一切ブロックしないと明記した |
| C1-2 | 稼働日カレンダー（土日祝日、複数直）を扱おうとすると、D0からの経過日数モデル（曜日概念が存在しない）と根本的に矛盾し、実装コストも跳ね上がる | 中 | §9.2で稼働能力は「1日あたり固定値」のみと明記し、カレンダー・シフトは対象外とした（§9.8残存リスクにも記録） |
| C1-3 | KPIダッシュボード（v5-spec.md §10の12指標、TC-17で期待値が固定されている）に能力指標を追加すると、既存の`computeKpi()`のテスト網羅性・期待値と衝突するリスクがある | 中 | `kpi.ts`は変更せず、独立した新規モジュール`domain/capacity.ts`・新規パネル`CapacityPanel.tsx`として追加する方針をEXT-31に明記した（Phase 2-AでCostPanelを独立させた判断＝EXT-16と同じ） |

### 9.7 実装可能性・整合性レビュー（第2周：データ整合性）

| # | 指摘 | 深刻度 | 対応 |
|---|---|---|---|
| C2-1 | 計画負荷の数量に`wi.inputQty`をそのまま使うと、第1工程以外は着手前`inputQty`が0のままのため（`production.ts`の`nextWi.inputQty = goodQty`は前工程完了時にしか設定されない）、後工程の計画負荷が常に0と計算されてしまう | 高 | 未着手工程の計画負荷数量は`mo.planQty`を使うと9.4に明記した。MRP本体も歩留まり100%前提（BOM展開に歩留まり率という概念が無い）であり、一貫した前提である |
| C2-2 | `WorkCenter`に新規必須フィールドを追加すると、EXT-26のJSON入出力（`MasterSnapshot`、all-or-nothing検証）が、過去にエクスポート済みのファイル（このフィールドを持たない）を受け付けなくなる | 中 | EXT-32に明記のとおり、欠落時デフォルト補完のような特別扱いは追加せず、検証エラーとして扱う（既存の`requireNonNegative`の一貫性を優先。セッション限りの教材であり実害は限定的と判断） |
| C2-3 | 山積み超過をAlertBarに出す際、確定前の計画オーダ（PLANNED_ORDER）由来の負荷を無視すると「確定してから初めて超過に気づく」という手遅れなタイミングになり、CRPの本来の価値（確定前に見て判断する）を活かせない | 中 | MVPでは確定済みオーダ（WORK_INSTRUCTION）のみを対象とする。PLANNED_ORDERはMRP再実行のたびに全削除・再生成される揮発データであり、確定前プレビューには「未確定な工順展開」という新しい計算経路が必要になるため、実装コストが可視化の枠を超える。§9.9で任意の拡張候補として切り出す |
| C2-4 | AlertBarへ山積み警告を追加する際、`AlertNavigateTarget`型（現状`"planning" \| "master-data"`）にどの遷移先を割り当てるかが未定義 | 低 | 新設の`"capacity"`タブへ遷移させる。日程遅延警告が個別オーダの操作（発注・製造）で解決しうるのに対し、能力超過はマスタ（作業区の稼働能力見直し）や作業配分という人の判断で解決する性質の警告のため、詳細を確認できる専用タブへ誘導する |

### 9.8 残存する簡略化・限界

v5-spec.md §15に倣い、本節で採用した簡略化を明記する。

| # | 内容 | 影響 |
|---|---|---|
| L-C1 | 計画負荷はオーダ単位で着手日へ一括計上する近似であり、工程ごとの実際の日程差（例：FG-100の工程10と工程20が本当は別日になる）は反映しない | 小。MFG_ORDER自体が`startDay`/`dueDay`という単一区間しか持たないため、本システムの日程管理粒度（オーダ単位）に合わせた結果であり、CRP固有の限界ではない |
| L-C2 | 稼働日カレンダー・複数直・段取り時間は扱わない | 中。§11.1の表が「ボトルネックと段取り」を挙げているうち、段取り時間は本設計の対象外のまま残る |
| L-C3 | 確定前（PLANNED_ORDER）段階での山積みプレビューはMVPに含めない | 中。§9.7 C2-3・§9.9参照 |

### 9.9 任意の拡張候補（MVPスコープ外）

- 計画オーダ（PLANNED_ORDER）段階での山積みプレビュー（確定前に見せる、C2-3参照）
- 段取り時間（`RoutingStep`に段取り時間フィールドを追加）を負荷計算に含める
- 山積み表を`GanttChartPanel.tsx`と同様のSVGバー表示へ発展させる（MVPは`.panel__table`ベースの表で十分。既存のKPI・原価パネルもすべて表ベースであり、視覚表現の追加は必須ではない）

### 9.10 実装コスト評価

Phase 2-A（原価）と同様、**既存テーブルへのフィールド追加＋独立した集計ロジックの追加**で完結し、既存の
状態遷移・数量計算（MRP・バックフラッシュ・引当）を一切変更しないため、**破壊的変更にはならない**。
`domain/capacity.ts`は他モジュールから読まれる依存先を持たず（`schedule.ts`・`kpi.ts`等からは呼ばれない、
独立した末端モジュール）、既存84件のテストへの影響はゼロと評価する。唯一の非後方互換はEXT-32のJSON入出力
（旧エクスポートファイルの再インポート不可）のみである。
