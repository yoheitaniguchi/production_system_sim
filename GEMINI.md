# GEMINI.md

このファイルは Antigravity / Gemini がこのプロジェクトで作業する際に参照するプロジェクトガイドラインおよび自律的判断ルールです。

## 1. プロジェクト概要 & 仕様参照

生産管理（受注〜出荷）のドメイン連携を学ぶための、動くミニマムシミュレーター。
木製イス（`docs/v5-spec.md` §1.1、2階層BOM・購買3品目・内製2品目・工順3行）を題材に、受注を入力すると7ドメイン（受注・計画・発注・工程・在庫・出荷・マスタ）へ情報が伝播し出荷に至る様子を、MRP・工程管理・ペギング・KPI・原価・ロット追跡まで含めて可視化する。

- 対象読者：開発チームメンバー（商用製品ではなく教材）
- 仕様の一次資料：
  - `docs/v5-spec.md`：業務仕様の一次資料（直接編集しない）
  - `docs/design.md`：v5仕様書との差分・追加決定・実装方針（★まず読む）

## 2. 技術スタック・アーキテクチャ制約

- **フレームワーク**: React 18 + TypeScript + Vite
- **バックエンド**: なし（ブラウザ完結、永続化なし、`useReducer` による単一セッション状態管理）
- **ドメインロジックの分離**:
  - ドメインロジックはすべて `src/domain/` 配下に配置し、**純粋関数**として実装する。
  - UIコンポーネント（`src/components/`）内に直接ドメイン計算や状態更新ロジックを書かない。
  - すべての状態変更は `src/domain/reducer.ts` の action 経由で行う。
- **操作粒度**: v5仕様書のユースケース単位。
  - 「日を進める」は日付を+1するだけ。MRP実行・発注・工程着手/完了・入荷・出荷引当などは個別のアクション。
- **不変性の保持**:
  - 状態オブジェクト（`SimulationState` 等）は常にイミュータブルに更新する（直接のプロパティ書き換えは禁止）。
- **BOM探索の安全性**:
  - BOMを再帰的に辿るコードを作成・修正する場合は、循環データによる無限再帰を防ぐため、必ず訪問済み集合または深さ上限を設けること。

## 3. プロジェクト標準コマンド

| コマンド | 用途 |
| :--- | :--- |
| `npm test` | Vitest による単体・統合テスト全件実行 (必須) |
| `npx vitest run <path>` | 指定したテストファイル単体の実行 |
| `npx tsc --noEmit` | TypeScript 型チェック |
| `npm run build` | TypeScript 型チェック + Vite ビルド (必須) |
| `npm run dev` | ローカル開発サーバー起動 |
| `npm run preview` | ビルド成果物のローカルプレビュー |

## 4. コマンド実行の自律判断ルール

Gemini / Antigravity はユーザーから明示的な指示がなくても、以下の状況に応じて自律的にコマンドを実行・検証してください。

### ① 型チェックの実行 (`npx tsc --noEmit` / `npm run build`)
* **判断タイミング**: `src/**/*.ts` または `src/**/*.tsx` ファイルを追加・編集・リファクタリングしたとき。
* **行動指針**: ファイル編集完了時に実行し、型エラーがないことを確認する。エラーが発生した場合はエラー箇所を特定し、自律的にコードを修正する。

### ② テストの実行 (`npm test` / `npx vitest run <path>`)
* **判断タイミング**: `src/domain/` 配下のロジックや `src/data/` を変更したとき、または `*.test.ts` を追加・変更したとき。
* **行動指針**:
  - 変更したモジュールに関連するテスト (`npx vitest run src/domain/<module>.test.ts`) を即座に実行する。
  - 大規模な修正や複数モジュールにまたがる変更の場合は `npm test` で全件パスすることを確認する。
  - テストが失敗した場合は、仕様書（`docs/v5-spec.md`, `docs/design.md`）と照らし合わせて修正を行う。

### ③ ビルド検証 (`npm run build`)
* **判断タイミング**: 新しい画面コンポーネントの追加、ルーティング変更、依存関係更新、または一連の機能実装が完了したとき。
* **行動指針**: Vite ビルドが正常に完了することを確認する。

### ④ パッケージの追加 (`npm install <package>`)
* **判断タイミング**: 新機能の実装に外部ライブラリが必要と判断した場合。
* **行動指針**: 最小限の依存関係に留めつつ実行してよい。

## 5. 安全性ルール（禁止・確認必須事項）

以下のコマンドや操作は、ユーザーの明示的な指示がない限り**自律的に実行してはなりません**。

* **Git の破壊的・外部送信コマンド**:
  - `git push`（リモートへのプッシュ）
  - `git reset --hard`, `git clean -fd`, `git checkout .`（未コミットの変更破棄）
  - `git rebase`, `git push --force`
* **ファイルシステムの危険な操作**:
  - `rm -rf /` や広範囲な削除コマンド
* **永続プロセスの放置**:
  - 開発サーバー（`npm run dev`）などを起動する場合は、動作確認後に適切に停止またはバックグラウンド管理すること。

## 6. UI設計・レビュー規約

- コンポーネントはアクセシビリティ、操作導線、エラー時の具体的なガイダンスに配慮する。
- 新画面追加やレイアウト変更時は、`.agents/skills/ux-reviewer/SKILL.md` の観点（情報設計・操作導線・メッセージ・アクセシビリティ）に準拠する。
